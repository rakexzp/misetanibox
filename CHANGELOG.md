# Changelog

## v1.5.8

**Разгрузка GPU — теперь реально работает**
1. Прошлый тумблер «Разгрузка GPU» через переменную окружения не срабатывал: go-webview2 сам перезатирает эту переменную пустым значением. Переключено на штатную опцию Wails WebviewGpuIsDisabled (--disable-gpu) — работает в текущей версии, апгрейд не нужен. На Linux вебвью и так без GPU-ускорения.

## v1.5.7

**Фикс системного прокси**
1. В режиме системного прокси соединения больше не рвутся каждые ~5 секунд. Причина: watchdog переустанавливал прокси на каждом тике (сравнение уже-установленного шло байт-в-байт и не совпадало с форматом реестра), а каждая переустановка слала WinINet-уведомление, из-за которого браузеры сбрасывали соединения. Теперь проверка устойчивая + дебаунс переустановки.

## v1.5.6

**Разгрузка GPU (тумблер)**
1. Настройки → Оформление → «Разгрузка GPU интерфейса»: точечно отключает GPU-композитинг вебвью (Windows: --disable-gpu-compositing через WebView2; Linux: WEBKIT_DISABLE_COMPOSITING_MODE). Помогает, если видеокарта греется от интерфейса. Применяется после перезапуска. GPU остаётся доступен для тяжёлых операций — это не полное отключение.

## v1.5.5

**Мордочка-каомодзи**
1. Под названием Misetanibox появилась текстовая мордочка, которая реагирует на подключение: спит когда отключено (-_-, u_u), радуется при коннекте (^o^, ^q^, XD и т.п.) с прыжком, изредка моргает.

## v1.5.3

**Зеркало по HTTPS**
1. Обновления, каталог тем и смарт-ядро теперь ходят к зеркалу напрямую по HTTPS (валидный сертификат) — надёжнее в РФ (меньше обрывов на :80).

## v1.5.2

**Нагрузка на видеокарту**
1. Волна трафика пересчитывается реже и легче (900мс/96 точек → 2.2с/44), и останавливается, когда окно свёрнуто в трей. Непрерывные анимации ставятся на паузу в фоне. Заметно меньше нагрузки на GPU.
2. Настройки → Оформление → «Экономный режим анимаций»: полностью отключает волну и свечение орба для слабых/горячих видеокарт.

## v1.5.1 (Linux)

**Linux: ядро и TUN**
1. На Linux приложение больше не ищет виндовые имена ресурсов: ядро называется clash (без .exe), проверяется как ELF; wintun не требуется (TUN идёт через ядро ОС). Исправляет «clash.exe not found / wintun not found».

## v1.5.0

**Конструктор цепочек прокси**
1. Новый раздел «Цепочки»: собери маршрут из нескольких серверов подряд (вход → промежуточные → выход) через relay. Цепочка появляется в «Прокси-узлы» как «🔗 имя» и выбирается как обычный сервер. Узлы берутся из активной подписки.

## v1.4.2

**Порт локального прокси**
1. Настройки → Базовые настройки сети → поле «Порт локального прокси (Mixed Port)»: теперь порт SOCKS5/HTTP можно менять и он сохраняется (раньше поля не было — правки в YAML затирались регенерацией и сбрасывались на 7890). Применяется сразу.

## v1.4.1

**Мастерская — правки**
1. Покраска: пикер текста теперь меняет только буквы, фон — только фон/панели (раньше текст перекрашивал всё).
2. Публикация: фоны карточек корректно уходят в тему (подняты лимиты), поддержка анимированных GIF-фонов.

## v1.4.0

**Мастерская тем**
1. Новый раздел «Мастерская»: каталог авторских тем без регистрации. Устанавливай чужие темы одним кликом (цвета+фоны карточек+положение), лайкай, жалуйся.
2. «Опубликовать» — заворачивает твоё текущее оформление в тему и выкладывает в каталог.
3. Фикс: пользовательские цвета теперь применяются и в тёмной теме (перебивались классом .dark).

## v1.3.9

**Покраска интерфейса**
1. Настройки → «Оформление — цвета»: свои цвета акцента, текста и фона (пикеры + пресеты Reaper/Nord/Матрица/Сепия/Океан). Из трёх цветов выводится вся палитра, применяется живьём поверх светлой/тёмной темы. Задел под каталог тем (Мастерскую).

## v1.3.8

**Флаги**
1. Убран дубль: если у сервера есть эмодзи-флаг в имени, в тексте он больше не показывается буквами рядом с SVG-флагом.

## v1.3.7

**Флаги стран**
1. Флаги теперь рисуются картинками (SVG), а не эмодзи-шрифтом — работают всегда: страна определяется по имени сервера (эмодзи, код, англ./рус. название).
2. Обновлён README (Windows+Linux, актуальные фичи).

## v1.3.6

**Галерея фонов**
1. «Мои фоны» в редакторе карточки: сохраняй свои картинки в личную галерею и применяй на любую карточку одним кликом (＋ добавить, ✕ удалить, «сохранить текущий»).
2. Linux-версия (отдельная сборка, фаза 2): добавлен TUN.

## v1.3.5

**Мелочи**
1. Окно редактора фона карточек теперь перетаскивается за заголовок — удобно отодвинуть и видеть карточку вживую.

## v1.3.4

**Пресеты фонов**
1. В редакторе фона карточек — набор готовых градиентов-пресетов (клик — и фон применён), помимо загрузки своей картинки.

## v1.3.3

**Исправления**
1. В редакторе фона карточек заработали ползунки положения и масштаба (перебивались CSS).

## v1.3.2

**Редактор фона карточек**
1. Клик по ✎ на карточке Консоли открывает редактор: положение картинки, масштаб и затемнение — всё живо применяется.

## v1.3.1

**Обновления из приложения**
1. Проверка и загрузка обновлений теперь идут через зеркало (HTTP, доступно из РФ) вместо GitHub API — встроенный апдейтер снова работает.
2. Пер-карточные картинки на экране «Консоль»: своя картинка на каждую плитку (наведи на карточку → ✎).

## v1.3.0

**Умное ядро (Smart)**
1. Опциональная установка ML-ядра (кнопка в Настройках → Поведение): выбирает лучший сервер по обученной модели — учитывает задержку, успешность и нагрузку, а не только пинг. Скачивается отдельно, стоковое ядро сохраняется для отката.
2. `sticky-sessions`: трафик к одному адресу закреплён за одним узлом — смарт не меняет сервер посреди игры или сессии.
3. «Использовать Смарт для всего» (Про): весь прокси-трафик идёт через смарт-выбор, а прямые правила (РФ-сайты, блок рекламы) сохраняются.

**Простой режим (Lite)**
1. Мобильный интерфейс: одна кнопка подключения через TUN, узкое окно, список серверов.
2. Добавление подписки прямо в Lite — по ссылке или через DNS TXT-запись (обход блокировки адреса подписки), имя тянется из заголовков.
3. Переключатель Lite/Про в шапке окна; окно само подстраивает размер под режим.

**Интерфейс**
1. Флаги стран у серверов (шрифт флагов вшит — Windows их иначе не рисует).
2. Анимация замера задержек в списке серверов.
3. Свой фон экрана «Консоль» — загрузка своей картинки под карточки (Настройки → Поведение).
4. Служебные группы с `hidden` больше не показываются в списке прокси.
5. Новый логотип (автор [@whxteangel](https://t.me/whxteangel)).
6. Снижено фоновое потребление памяти в Lite (ядро не запускается, пока не подключишься).

## v1.2.3

**Простой режим (Lite)**
1. Новый упрощённый интерфейс в стиле мобильного приложения: одна кнопка подключения, список серверов с проверкой задержек, компактная колонка по центру.
2. Подключение одним тапом через TUN (полный перехват трафика) в глобальном режиме; при первом запуске автоматически предлагается установить фоновую службу.
3. Переключение между «Про» и «Lite» в Настройках → Поведение; выбор сохраняется между запусками.

**Маршрутизация по приложениям**
1. Новая вкладка «Приложения» в разделе «Правила»: список установленных программ (из меню Пуск) с иконками, поиском и ручным выбором `.exe`.
2. Два режима: «Мимо VPN» (выбранные приложения идут напрямую) и «Только через VPN» (через VPN идут только выбранные).
3. Правила `PROCESS-NAME` подмешиваются автоматически; при включении фичи принудительно используется режим маршрутизации по правилам.

**Идентификация устройства (HWID)**
1. Передача заголовков `x-hwid`, `x-device-os`, `x-ver-os`, `x-device-model` при загрузке подписки.

**Интерфейс и локализация**
1. Полный перевод интерфейса на русский язык: все страницы, трей, диалоги, сообщения об ошибках, журнал приложения и установщик.
2. Исправлено наложение текста из-за моноширинного шрифта — основной шрифт заменён на пропорциональный.
3. Обновлён брендинг и логотип приложения.

## v1.2.2

**网络探测与状态联动机制重构**
1. 彻底重构出站 IP 检测机制为路由感知（Route-aware）模式，强制通过内部代理端口测试，解决 TUN 模式下底层的延迟或 IPv6 泄漏导致的直连 IP 误判问题。
2. 引入平滑去抖与静默更新策略，在网络路由切换期间保留上一次成功的过期 IP 数据，拒绝无脑清空，彻底消除控制台状态闪烁。
3. 对齐流量卡片与控制台卡片的显示模型，引入 `intent/actual/pending` 三层状态模型，移除多余的中间过渡状态，提升 UI 操作的确定感。
4. 增强了流量面板中手动刷新 IP 按钮的鲁棒性，对接新的防跳闪逻辑，避免手动强制刷新引发的意外回退。

**系统服务与安装链路优化**
1. 深度优化 GoclashZHelper 系统服务，降级 SCM 服务访问权限，修补潜在的 DACL 安全漏洞，提升权限控制安全性。
2. 完善 Helper 服务的自愈恢复与随主进程退出机制，解决安装过程中的进程强杀与残留问题，实现服务的无感守护。
3. 重构内核资源的目录结构，解耦安装目录与运行时数据目录，优化 Seed 文件驱动的自我修补与环境恢复流程。
4. 修复由于提权文件路径冲突和编码导致的问题，确保便携包与安装包的极速平滑迁移，即使缺失初始元数据亦能自动兜底修复。

## v1.2.1

**核心架构重构与提权分离**
1. 实现独立的 Helper 服务（Named Pipe IPC），分离管理员提权逻辑，负责核心进程管控及系统环境设置。
2. 彻底分离程序二进制与用户数据目录，支持便携模式平滑迁移，解决开机自启由于权限或路径导致的失效问题。
3. 弃用原管理员权限自启机制，统一采用普通权限启动与提权通信，大幅提升冷启动性能。
4. 增强安全性，增加 Helper 服务的快速自我修复与基于用户 SID 的访问控制 (ACL)。

**网络探测与稳定性提升**
1. 全新引入出站网络双栈（IPv4/IPv6）检测支持，并发执行与 First-Valid 快速返回，提高探测速度与准确度。
2. 重构内核状态调和机制（Reconcile），增强 Ping API 探针容错率，彻底隔离多端并发引发的延迟异常。
3. 完善 TUN-aware 分流机制，确保 TUN 模式运行状态显示精准，且具备更强的启动容灾能力。

**UI 界面与交互优化**
1. 界面新增内核日志的来源分类展示与出站双栈 IP 的探测结果详情。
2. 修复配置编辑器在暗色主题下文本选中时样式反色的异常，全局统一黑白主题风格。
3. 更正 DNS 设置中存在歧义的“启用 DNS 解析”开关，修正为“启用 DNS 覆写”并完善提示。
4. 清除冗余的乐观 UI 组件及老旧数据目录结构残留，增强向前兼容性。

## v1.2.0

**规则模块重构与界面优化**
1. 重构规则存储引擎，移除遗留的 dirty 标记机制，极大简化了远程配置覆写逻辑。
2. 规则表单全面升级为结构化下拉选择（类型、内容、策略）。
3. 策略下拉框新增支持选择所有底层节点及 GLOBAL/COMPAT 内置策略。
4. 增强规则引擎校验机制，强制规范 MATCH 类型及常规规则格式，允许兼容解析自定义类型。

**并发安全与稳定性修复**
1. 修复规则配置混用问题，在统一锁区间内读取快照与覆盖规则，彻底解决高频更新导致的死锁与脏读。
2. 优化订阅更新防擦写机制，将网络下载、覆盖写入、缓存初始化等流程集成至单次事务。
3. 修复了附加规则配置（overlay）加载时，错误吞没文件损坏导致静默清空的隐患。
4. 清理前端冗余代码，移除陈旧的计算属性与多余的样式绑定。

**发版集成与打包自动化**
1. 新增基于 GitHub Actions 的 Release CI 工作流，推送 v* 标签自动构建并发布。
2. 引入极致精简的便携包（ZIP 与 7z），仅保留主程序与内核，支持首启自动释放环境。
3. 规范化 Inno Setup 配置，安装包固定输出为 GoclashZ_win_amd64_Setup.exe。
4. 新增版本管理脚本 set_version.ps1，实现版本号统一自动化注入。
